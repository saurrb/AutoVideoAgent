# Grok Image CLI Tool

## What it does
Generates one image from prompt text using Grok CLI.

## Copy-paste command (CMD)
```bat
python "C:\Users\Saurabh\Documents\AutoVideoAgent\pages\automation_tools\grok_image_cli\grok_cli_image_generate.py" --prompt-file "C:\path\image_prompt.txt" --output-dir "C:\path\image_outputs"
```

## Notes
- Portrait 9:16 composition intent
- Writes `grok_image.done.json` in output folder
